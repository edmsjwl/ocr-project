# CLAUDE.md — SBS 영수증전자화

이 문서는 Claude Code가 이 프로젝트에서 작업할 때 매 세션 시작 시 읽는 **영구 지침 파일**이다. 디자인 토큰·컴포넌트 스펙 같은 디자인 시스템 세부사항은 여기 두지 않는다 — 그건 `DESIGN.md`가 단일 출처(source of truth)다. 이 파일은 **원칙과 규칙**만 담는다: 매번 반복 설명하고 싶지 않은 것, 팀 새 멤버도 알아야 할 것.

> 참고: 아래 "디자인 도메인 규칙", "Build & Test", "Repo 구조" 섹션은 아직 확정 내용을 채우지 못했다. `[ ]`로 표시된 항목은 Figma·저장소 설정이 갖춰지는 대로 채워 넣을 것 — 없는 값을 지어내는 것보다 비워두는 편이 낫다.

## 프로젝트 개요
SBS 영수증전자화 — 영수증을 촬영/스캔해 OCR로 인식하고 정산·내역 관리를 돕는 모바일 앱(iOS 375px 프레임 기준).

## Source of Truth
- **디자인 토큰·컴포넌트 스펙**: `DESIGN.md` (Figma "🔵 디자인시스템" 페이지 기준). 색상/타이포/spacing/radius를 코드에서 새로 정의하지 말고 이 문서의 토큰을 참조한다.
- **디자인 원본**: Figma `[SBS] OCR 프로젝트_디자인` 파일. 화면 스펙이 모호하면 코드를 짜기 전에 Figma를 다시 확인한다.
- 두 문서가 어긋나면 Figma(최신 디자인)가 우선이고, 그 사실을 알려서 `DESIGN.md`를 갱신해야 한다.

## 원칙

### 디자인 충실도
- 새 컴포넌트를 만들기 전에 `DESIGN.md`와 기존 코드에 이미 있는 컴포넌트/패턴이 있는지 먼저 확인한다. 있으면 재사용, 없으면 그때 새로 만든다.
- 색상·spacing·radius·그림자를 하드코딩하지 않는다. 토큰/변수로 참조한다.
- 브랜드 액센트(Clear Blue)는 화면당 하나의 주요 액션에만 쓴다 — 새로운 강조색을 임의로 추가하지 않는다.
- 상태 표시(성공/경고/진행/정보)는 `DESIGN.md`의 4가지 Status 토큰만 쓴다. Color 레퍼런스 페이지의 풀 채도 램프를 직접 끌어다 쓰지 않는다.

### 정확성 우선
- 확인되지 않은 사실(빌드 명령어, API 스펙, 파일 경로 등)을 지어내지 않는다. 모르면 모른다고 말하고, 확인 방법을 제안한다.
- Figma 노드 ID, 파일 경로, 커밋 메시지 등은 실제로 조회한 값만 인용한다.

### 작업 방식
- 큰 변경(아키텍처 변경, 의존성 추가, 여러 화면에 걸친 리팩터링) 전에는 계획을 먼저 설명하고 확인받는다.
- 사용자가 명시적으로 요청하지 않으면 git commit을 만들지 않는다.
- 같은 실수를 두 번째로 지적받으면, 그 교정 내용을 이 파일(또는 `.claude/rules/`)에 추가해 다음 세션부터 반복되지 않게 한다.
- 문서(`DESIGN.md`, `CLAUDE.md`)와 실제 코드가 어긋나기 시작하면, 조용히 넘어가지 말고 어느 쪽이 최신인지 확인 후 갱신한다.

### 커뮤니케이션
- 사용자와의 대화는 한국어로 한다.
- 코드/커밋 메시지/변수명 등은 [ ] 프로젝트 컨벤션 확정 후 기입 (예: 영어 사용 여부, 네이밍 컨벤션).
- 애매한 요청은 추측해서 진행하기보다 먼저 확인 질문을 한다. 단, 되돌리기 쉬운 작업은 합리적으로 가정하고 진행 후 가정한 내용을 명시한다.

## 디자인 도메인 규칙
영수증 상태, 카드 유형, Web 조회 화면(Table/Filters) 등 업무 도메인에 특화된 디자인 규칙이 들어갈 자리다. Figma에 해당 화면/컴포넌트가 실제로 정의되는 대로 이 섹션을 채운다 — 정의되지 않은 내용을 앞서 채우지 않는다.

- [ ] Receipt Status — 상태 값 목록과 Label/Color/Icon 표현 규칙
- [ ] Card Types — 카드 유형 구분과 Status UI와의 구분 기준
- [ ] Data Table — Web 조회 화면 컬럼 우선순위, 정렬/정합 규칙
- [ ] Filters — 조회 조건 구성과 필수/보조 위계
- [ ] Duplicate 처리 — 중복 제출 표시·필터링 규칙
- [ ] Forms — 입력 화면 규칙
- [ ] UX Writing — 톤/CTA 문구 규칙
- [ ] Component 명명 규칙
- [ ] Accessibility 규칙

## Build & Test
- [ ] 저장소 위치 / 클론 방법
- [ ] 패키지 매니저 및 설치 명령 (예: `npm install`, `pnpm install` 등)
- [ ] 빌드 명령
- [ ] 테스트 명령 (커밋 전 반드시 실행할 것으로 지정할 명령)
- [ ] 린트/포맷 명령

## Repo 구조
- [ ] 주요 디렉터리 구조 (예: 화면/컴포넌트/디자인 토큰 코드가 위치하는 경로)
- [ ] 디자인 토큰이 코드화되는 위치(예: 테마 파일, CSS 변수, 상수 파일 등) — 정해지면 `DESIGN.md`의 토큰과 1:1 대응되게 유지

## 관련 문서
- `DESIGN.md` — 디자인 시스템 레퍼런스 목차·표기 규칙 (실제 색상/타이포/spacing/컴포넌트 스펙은 `docs/design-system/01-foundation.md`~`06-display-feedback.md`에 있음)
- Figma: `[SBS] OCR 프로젝트_디자인` (🔵 디자인시스템 페이지, node 39:2)
